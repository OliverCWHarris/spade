module.exports = {
	name: '',
	aliases: ['commands'],
    cooldown: seconds,
	execute(message, args) {
		message.channel.send();
	},
};


} if (command === '') {
        client.commands.get('').execute(message, args);
    }